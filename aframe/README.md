# A-Frame

There was former A-Frame code here, however it was not using locar.js and thus has been removed, it can be found by going back to commit `01f7708`.

As soon as the pure three.js version is doing something interesting, an A-Frame wrapper will be recreated.
